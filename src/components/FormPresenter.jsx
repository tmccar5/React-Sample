import React, { PropTypes } from 'react';

const FormPresenter = ({

}) => {
  const styles = {
    
  };

  return (
    <div>
      Tomas
    </div>
  );
};

FormPresenter.propTypes = {

};

export default FormPresenter;
